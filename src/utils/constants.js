export const APP_ROUTE = {
	DEFAULT: '/',
	ENTRY: "*",
}

export const POKES_API = {
	GET_POKES: "/pokemon/",
}

export const URLS_PROCESSED_IN_COMPONENTS = [
	APP_ROUTE.ENTRY,
];

export const URL_NAME_CATEGORY = {
	GENERATION: 'generation'
}